# Monitor plan

PDCA. This file is the Plan step of the monitor turn in
`loom-weave-godot`. The walk packet of 2026-09-04 is the daydream
this Plan reacts to. It is not a spec of its own.

The workItem is `monitor`.

## Target

Put a read-only monitor on the window so Seth can see where he is.

- **Top border: the path spine.** This repo → the focused node.
  Grows as you go in. Always visible. How you get back out.
- **Under the spine: the PDCA line.** Every open workItem that
  carries a `pdca` prop shows Plan, Do, Check, or Act. That is
  the thing the walk could not see.
- **Middle: this repo's tree.** Click a node. The right side
  shows what was done, what is next, and the body. Read-only.
- Gear stays. Loadout stays. The renderer does not write.

Cycle two ruled a black first screen. That ruling stands until
this Do puts the monitor in the content area. The backdrop stays
black. The cards do not come back.

## This Do

First visible. Not the whole walk.

- Spine, PDCA line, tree, node detail.
- State colour: live ink, done and abandoned dim (the ghost
  reading we can do without a new field).
- Tree is this weave. Galaxy click and cross-repo pointers wait.
- No refresh hook. No Worker socket. No timer.
- No write. No branch-from-node.

## Not this cycle

- Picking post-commit hook vs Cloudflare Worker.
- Memory-budget checker.
- RAG.
- Iteration counter (OPEN in loom-warp).
- Writing from the renderer.
- Apollo hook.
- Host-map changes. Apex left alone.
- Schema edits in loom-warp.

## Success bar

The window is no longer an empty black field. The spine names
this operation. Open work is labeled Plan, Do, Check, or Act.
Clicking a past node shows its close-out prose. The gear still
opens the loadout. A write from this view is a fail.

## After

Owner sees it. That is the Check. Act waits on that.
