# Findings — cycle one

Interface notebook. Append. Do not rewrite a beat. A beat that
hardens becomes an issue node under the work it hit.

## Schema watch

Nothing yet that blocks `loom-warp`. Cross-repo pointers remain
OPEN in warp. Close-out on a repo with no tree remains OPEN; this
repo has a tree, so it is not that question.

Do not invent a host or deploy type. Instantiation one is a
`workItem`. The live site is an `artifact` when it exists.

## Beats

### 2026-09-03 — Godot 4 web headers

Threaded export needs HTTPS plus COOP `same-origin` and COEP
`require-corp`, or threads off, or Godot's PWA service-worker
workaround. Plain GitHub Pages cannot set those headers. Hits host,
not schema.

### 2026-09-03 — Domain is owner

Cycle one needs a name. The plan does not pick one. Hits process.
Blocks the Do, not this Plan step.

### 2026-09-03 — Instantiation one is not self-render

The card already said the first screen stays unruled until
instantiation one is on screen. Cycle one Do is the empty export on
HTTPS. `self-render` waits. Hits process.

### 2026-09-03 — PDA is PDCA

Speech said PDA. `PROCESS.md` and the cards say PDCA. Same loop.
Hits nothing.
