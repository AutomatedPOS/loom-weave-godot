# Findings — first mark from white

Date: 2026-09-05. Seat: Grok, remote sitting. Packet only.
`weave/` not touched. Owner word before Fable builds.

Picture first: `artifacts/findings/2026-09-05-first-mark.svg`.

## Beats

### 2026-09-05 — Picture

White sheet. One empty tile. Hollow, ink stroke, no fill, no
accent, no caption. Top-left of the field, large enough to be the
one thing on the window.

This is bible 4.3 / 4.5 / 4.6 / 4.14 as the smallest loop from
`BLANK`: a squared shell, not started, something should exist
here, one element dominates. It is not the black close-out. It is
not rails-and-ports. It is not a glyph drawing. The inner mark is
a later loop.

### 2026-09-05 — Fable job, if the owner takes the picture

One job. Do not add a second.

1. Token `MARK` in `weave/theme/Tokens.gd`: 320, the tile on this
   sheet. Stroke is `LINE_W`. Colour is `INK`. Fill is none.
2. Draw that one rect on `BLANK`. New script under `weave/`, one
   Control on `Interface`, visible. Canvas, monitor, gear, loadout
   stay hidden.
3. `first_screen_smoke`: backdrop `BLANK`, the mark present and
   visible, the four paused nodes still off the window.
4. Capture 1440×900 with `weave/capture.gd`. Put it next to this
   file as `2026-09-05-first-mark.png`.

Do not restore `Here.gd`. Do not name the tile. Do not mint a
Comfy glyph into it. Do not show TASK until something on the
field is the current task.

### 2026-09-05 — Live

Worker `dord-dev` refreshed this sitting. Hard-refresh
`https://loom.dord.dev/` is still the white sheet. The tile is
not on it until the owner takes the picture.

## Close-out

- **justDid**: Picture of one empty tile on the white sheet.
- **next**: Owner word on the picture. Then Fable builds that one
  job, or the picture comes off.
- **waitingOn**: The owner's look.
- **generic**: From a pause sheet, the first loop is one shell,
  not the last composition and not the rejected close-out.
