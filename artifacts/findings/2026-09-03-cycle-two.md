# Findings — render cycle two

Date: 2026-09-03. Seat: Grok, cloud agent
`bc-5bd71729-6229-459f-a899-60c97a214802`. Owner sitting outside.
No desktop. No self-hosted worker on this run.

## Beats

### 2026-09-03 — Do includes push

Owner, from a tablet: commit and push during the Do, not after
Check. Unpushed work cannot be seen from the tablet. Hardened as
issue `do-includes-push`. Hits process.

### 2026-09-03 — First screen named

Owner named cycle two from outside: strip the turn-one forms, load
Godot on a black screen, leave one settings gear on the interface
track. Bottom-right, the usual chrome corner. Light, subdued gray.
That is the ruling cycle one deferred.

### 2026-09-03 — Self-render stays off the window

Turn one marked `self-render` done when the tree cards were on
screen. Cycle two takes the cards off. The loader and card script
stay in the repo. The node is not reopened here; the window no
longer shows that view.

### 2026-09-03 — Gear does not open a panel

The gear is chrome. What it opens is unruled. Do not invent a
settings form this cycle.

### 2026-09-03 — Host map untouched

No deploy, no DNS, no Pages. This turn is the Godot window.

### 2026-09-03 — Act closed

Owner: works perfectly. Cycle two Act is closed. Next named:
loadout. Chat, TTS, transcription. Configurable after deploy. Save
on the machine. Export and import. Not in the base install. Plan is
`artifacts/cycle-three-plan/`.

### 2026-09-03 — instantiateStreaming hang

Hard refresh still froze. Godot clones the wasm Response, keeps
`Content-Encoding: gzip`, then `instantiateStreaming` on already
decoded bytes. Safari hangs on the splash. Patch: force the
arrayBuffer path and drop Content-Encoding on the clone.

### 2026-09-03 — Tablet freeze on splash

Refresh stuck on the Godot load. Live `index.wasm` is stored gzip.
A tablet sends `Accept-Encoding: gzip`; Workers gzip it again. One
decode leaves gzip, not wasm. `instantiateStreaming` never finishes.
Fix: `workers/serve.mjs` serves `/index.wasm` with `encodeBody:
manual` and one `Content-Encoding: gzip`.

### 2026-09-03 — dord-dev uploaded

Owner token authenticated. `./deploy-weave.sh` uploaded assets to
worker `dord-dev`. Version `44f60ee1` is 100%. Live
`https://loom.dord.dev/index.html` and `index.js` match the cycle-two
export. Route attach failed (token has no zone Workers Routes).
Domain was already bound. Apex left alone. Refresh is the Check.

### 2026-09-03 — Deploy still blocked

Owner asked to deploy so the tablet can test. Git is on the remote
(`1f8b536`). The Cloudflare token from the earlier Pages sitting
returns 9109 invalid. Temporary preview cannot take the wasm
(7.9 MiB gzip, 5.5 MiB brotli; preview cap is 5 MiB).
`https://loom.dord.dev/` is still the old cards. Apex left alone.
A valid `CLOUDFLARE_API_TOKEN` on this seat is the remaining gate
for `./deploy-weave.sh`.

### 2026-09-03 — Go, live weave blocked

Owner said go. Native window is the first screen. Web export built.
`wrangler deploy` of `dord-dev` failed: this seat has no
`CLOUDFLARE_API_TOKEN`. `https://loom.dord.dev/` is still the old
cards. Apex left alone. Deploy path is `./deploy-weave.sh`.

### 2026-09-03 — Check

`first_screen_smoke.gd` passed. Window capture: black field, no cards,
no bar, one gray cog bottom-right. Tree-loader smoke still needs a
loom checkout; that is not this cycle's bar.
