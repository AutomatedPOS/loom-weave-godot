# Findings — shape store

Date: 2026-09-05. Seat: Grok, cloud sitting bound to LOOP.md.
Source: canvas-plumbing shape store, owner Check accepted, owner
said continue. Plumbing in `weave/`. No restyle. No port labels.

## Beats

### 2026-09-05 — Query on the browser

`weave/Shape.gd` is the GDScript twin of `scripts/canvas_model.py`
allowlist. A body, a credential, a transcript, or a window id that
is a guid fails. Store is `user://shapes/current.json`, same class
as the loadout, for shapes.

The canvas writes that file when the seat moves or a chip docks or
leaves. It does not write a `thread.json`. The file is windows and
attachments: the seat as `ask.kind = node`, docked chips as roster
guids onto that window. Refresh instantiates a second canvas and
the dock is still there.

No new chrome. Ports stay unlabeled. OPEN 3 is not guessed. File
export of a shape (the second store) waits on that ruling; the
allowlist already dumps JSON a later sitting can hand to a file
picker.

### 2026-09-05 — Not this pass

Persona bind. Talking to a model. Port names. Sky / water / middle.
A tool. A process besides brief. Inbox format.

Close-out:

- **justDid**: Query store on the browser. Seat and docks survive a
  reload. Six smokes plus `shape_smoke`.
- **next**: Owner merge, deploy `dord-dev`, hard-refresh. File
  export of a shape still waits.
- **waitingOn**: Owner Check. OPEN 3.
- **generic**: Persist the ask, not the rows.
