# Daydream packet — Weaver PDCA cycle one

Talk only. Do not edit a repo. Do not mint a domain. Do not pick a
first screen. Findings come back to the interface in
`loom-weave-godot`, filed at `artifacts/findings/`.

2026-09-03. Cycle one Plan step. Instantiation one is the Do, not
this sitting.

Read with: `artifacts/cycle-one-plan/cycle-one-plan.md`,
`artifacts/slot-spec/slot-spec.md`, `loom/PROCESS.md`.

## Locked — do not reopen

- Render loop is continuous PDCA. A spec is the plan step of a turn.
- Promote-then-comply. Work in `loom-weave-godot`. Promote to
  `loom-weave` after it proves out.
- Slot spec is done. Depth is discrete slots. Backdrop and interface
  tracks are bookends, not numbers.
- First screen stays unruled until instantiation one is on screen.
- Self-render reads loom's own tree. Not this cycle's Do.
- Apollo hook is parked.
- Nodes are impersonal. No new schema fields. Schema loop waits for
  a block.
- Three thumbs before an execution pass. Pain-32.

## Cycle one target

HTTPS site. Godot web export loads. Black screen is enough.

## Owner still holds

The domain name. Talk the shape. Do not pick the string.

## Talk through

1. One name for the weaver, or a name per weave. Who the registrar
   sits under.
2. Host that can do HTTPS. Godot 4 threads need COOP/COEP, or
   threads off, or the PWA worker workaround. Pages alone cannot set
   the headers.
3. What "Godot interface" means on load one, when the Interface
   track is empty. Empty canvas vs chrome. Do not rule the first
   screen.
4. Whether the public URL is the wasm itself or a shell around it.
5. What must exist before the Do: domain, DNS, cert, export
   templates, a host that matches the header choice.
6. Anything the slot spec or the warp schema will not survive once a
   real export is on a real URL. Write it down. Do not patch warp
   from the talk.

## How findings come back

A short list. Fact, then what it hits: host, Godot, schema, or
process. The interface files it. A hardened fact becomes an issue
node. A warp block is marked as a block, not a patch.

## Do not

Build. Commit. Invent a type. Name the domain. Describe the first
screen as if it were ruled.
